import React, { Component } from "react";
import Loading from "./loading.gif";

export default class Spinner extends Component {
  render() {
    return (
      <div>
        <img src={loa} alt="spinner image" />
      </div>
    );
  }
}
